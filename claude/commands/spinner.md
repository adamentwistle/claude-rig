---
description: Switch Claude Code spinner verb pack (list | <name> | random | daily | off)
---
Run the `spinner-pack` script in the Claude config dir's `bin` folder with `$ARGUMENTS` (default to `list` if no arguments): `~/.claude-work/bin/spinner-pack` on the Mac, or `python "$CLAUDE_CONFIG_DIR/bin/spinner-pack"` on Windows. Report the one-line result. Do nothing else.
