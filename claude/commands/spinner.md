---
description: Switch Claude Code spinner verb pack (list | <name> | random | daily | off)
---
Run `~/.claude-work/bin/spinner-pack $ARGUMENTS` (default to `list` if no arguments) and report the one-line result. Do nothing else.
