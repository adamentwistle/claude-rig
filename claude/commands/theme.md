---
description: Switch Ghostty + status line theme together (list | nightshade | rose-pine | kanagawa-wave | kanagawa-dragon | catppuccin-mocha)
---
Run `~/.claude-work/bin/ccs-theme $ARGUMENTS` (default to `list` if no arguments) and report the one-line result. Remind the user to press cmd+shift+, in Ghostty to reload. Do nothing else.
