---
description: Switch terminal + status line theme together (list | nightshade | rose-pine | kanagawa-wave | kanagawa-dragon | catppuccin-mocha)
---
Run the `ccs-theme` script in the Claude config dir's `bin` folder with `$ARGUMENTS` (default to `list` if no arguments): `~/.claude-work/bin/ccs-theme` on the Mac, or `python "$CLAUDE_CONFIG_DIR/bin/ccs-theme"` on Windows. Report the one-line result. On the Mac remind the user to press cmd+shift+, in Ghostty to reload; on Windows WezTerm reloads by itself. Do nothing else.
